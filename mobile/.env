# ─────────────────────────────────────────────
# VetConnect Mobile — Environment variables
# Variables MUST be prefixed with EXPO_PUBLIC_ to be
# exposed to the React Native runtime.
# ─────────────────────────────────────────────

# Backend REST API URL (Express + Prisma)
# En dev, src/lib/env.ts lo calcula solo desde el host de Metro (hostUri),
# asi `expo start` funciona en cualquier WiFi sin editar esto.
# Este valor solo se usa como fallback (tunnel, build EAS, etc.).
EXPO_PUBLIC_API_URL=http://10.20.31.104:3001

# WebSocket endpoint (Socket.io sin path custom; el /ws/queue era un fantasma)
EXPO_PUBLIC_WS_URL=ws://10.20.31.104:3001

# LiveKit server URL (cloud or self-hosted)
EXPO_PUBLIC_LIVEKIT_URL=ws://localhost:7880

# Optional — Cloudinary free tier for pet photo uploads
EXPO_PUBLIC_CLOUDINARY_CLOUD_NAME=
EXPO_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
