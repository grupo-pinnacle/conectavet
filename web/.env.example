# Copiar a .env con valores reales. NUNCA commitear .env.
VITE_API_URL=http://localhost:3001
# Origen del socket. En dev queda vacío y se usa window.location.origin
# (Vite proxya /socket.io al backend). En prod apuntar al backend:
# VITE_SOCKET_URL=https://api.tudominio.com